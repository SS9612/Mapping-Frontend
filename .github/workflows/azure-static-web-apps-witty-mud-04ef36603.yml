# DISABLED - Frontend is now deployed to Azure Web App, not Azure Static Web Apps
# This workflow was auto-created by Azure Static Web Apps but is now disabled
# To completely remove: Delete this file AND disconnect Azure Static Web Apps from GitHub in Azure Portal

name: Azure Static Web Apps CI/CD

# Completely disable all triggers - this workflow will never run automatically
on:
  workflow_dispatch:  # Only manual trigger, won't run on push/PR

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false  # This job will never run
    steps:
      - name: This workflow is disabled
        run: |
          echo "❌ This workflow is DISABLED"
          echo "Frontend is now deployed to Azure Web App via deploy-frontend-webapp.yml"
          echo "Please use the deploy-frontend-webapp.yml workflow instead"
